import styles from "../styles/About.module.css"
import styled, { css, keyframes } from "styled-components"

const Holo = () => {
    return (
        <div className=""></div>
    );
}

export default Holo;